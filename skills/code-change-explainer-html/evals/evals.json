{
  "skill_name": "code-change-explainer-html",
  "evals": [
    {
      "id": 1,
      "prompt": "Walk our Java engineers through this TypeScript refactor that converts a callback-based fetcher into async/await with optional chaining. Make a standalone HTML teaching document with a Java mental model, line-by-line walkthrough, concept callouts for `async`/`await`/`?.`/`??`, and before/after panels. Before:\n\n```ts\nfunction loadUser(id, cb) {\n  db.user.find({ id: id }, function (err, row) {\n    if (err) return cb(err);\n    cb(null, row && row.profile && row.profile.name);\n  });\n}\n```\n\nAfter:\n\n```ts\nasync function loadUser(id: string): Promise<string | null> {\n  const row = await db.user.findUnique({ where: { id } });\n  return row?.profile?.name ?? null;\n}\n```",
      "expected_output": "Single-file HTML teaching document delegating presentation to html-document (no redefined CSS already in elements.md). Includes: document type breadcrumb, title + audience, concept map, one lesson with Java equivalent + concept + Java mental model callout + strategy + before/after stacked panels + line-by-line walkthrough + concept callouts for async, await, optional chaining ?., nullish coalescing ?? with Java parallel and gotcha, gotchas list, verification. Print-friendly.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Create an HTML explainer for this Python change that replaces a config dictionary with a dataclass. Audience: Java engineers familiar with Spring. Use the skill's lesson anatomy. Before:\n\n```python\ndef build_limit(cfg):\n    return {\n        'rps': cfg.get('rps', 10),\n        'burst': cfg.get('burst', cfg.get('rps', 10) * 2),\n    }\n```\n\nAfter:\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Limit:\n    rps: int = 10\n    burst: int | None = None\n    def __post_init__(self):\n        if self.burst is None:\n            object.__setattr__(self, 'burst', self.rps * 2)\n```",
      "expected_output": "Single-file HTML teaching document deferring presentation to html-document. Includes lesson with Java mental model mapping @dataclass(frozen=True) to a Java record / immutable POJO, concept callouts for the decorator, default args, frozen-instance __post_init__ workaround, optional type `int | None`. Before/after stacked panels via html-document elements catalog. Gotchas covering mutable default args, frozen mutation trick. Verification step.",
      "files": []
    }
  ]
}
